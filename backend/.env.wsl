# Database Configuration
# For running scripts directly in WSL (not Docker)
# Use localhost to connect to WSL PostgreSQL

DATABASE_URL=postgresql://postgres@localhost:5432/budgetapp_dev

# Application Settings
APP_NAME=BudgetApp
APP_VERSION=2.0.0
DEBUG=True

# CORS Settings (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000
